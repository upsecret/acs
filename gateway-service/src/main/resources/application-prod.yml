logging:
  level:
    root: ${LOG_LEVEL:WARN}

spring:
  cloud:
    gateway:
      server:
        webflux:
          trusted-proxies: "${TRUSTED_PROXIES:10\\.100\\.0\\..*}"

management:
  tracing:
    sampling:
      probability: 0.1
