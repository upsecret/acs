logging:
  level:
    root: DEBUG
    org.springframework.cloud.gateway: TRACE

spring:
  cloud:
    gateway:
      server:
        webflux:
          trusted-proxies: ".*"

management:
  tracing:
    sampling:
      probability: 1.0
